[package]
name = "vbf-types"
version = "0.1.0"
edition.workspace = true
license.workspace = true
description = "Foundational identity, time, revision, and quantity types for VBF."

[dependencies]
serde = { version = "1", features = ["derive"] }
uuid = { version = "1", features = ["v7"] }

[dev-dependencies]
serde_json = "1"
